const CACHE='financial-navigator-v2';const BASE=new URL(self.registration.scope).pathname;const ASSETS=[BASE,`${BASE}manifest.webmanifest`,`${BASE}icon.png`];self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS))));self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;e.respondWith(fetch(e.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy));return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match(BASE))))});self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));
